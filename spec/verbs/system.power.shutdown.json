{
  "name": "system.power.shutdown",
  "strict": true,
  "description": "Initiates an OS-level shutdown. With `delay_seconds: 0`, the connection may drop before any response. With `delay_seconds > 0`, the OK with `scheduled_at` arrives before shutdown is issued. Extra-risky (X) verb; agents may require an elevated tier or explicit policy grant before honouring it.",
  "input_schema": {
    "type": "object",
    "properties": {
      "delay_seconds": {
        "type": "integer",
        "minimum": 0,
        "default": 0,
        "description": "Seconds to wait before initiating shutdown. Zero means immediate."
      },
      "force_close_apps": {
        "type": "boolean",
        "default": false,
        "description": "Force-close applications without prompting. Maps to EWX_FORCE on Windows."
      },
      "reason": {
        "type": "string",
        "description": "Free-form reason logged with the shutdown event. Surfaced in Windows Event Log."
      }
    },
    "additionalProperties": false
  },
  "x-crudx": "X",
  "x-since": "2.0",
  "x-renamed-from": "system.shutdown",
  "x-namespace": "system",
  "x-output-schema": {
    "type": "object",
    "properties": {
      "scheduled_at": {
        "type": "string",
        "format": "date-time",
        "description": "ISO 8601 timestamp of when the OS-level shutdown will fire. Equals now+delay_seconds. Only present when delay_seconds > 0."
      }
    },
    "additionalProperties": false
  },
  "x-errors": ["insufficient_privilege", "policy_blocked"],
  "x-implementations": [
    {
      "name": "initiate_system_shutdown_ex",
      "detect": { "type": "always" },
      "available_on": ["windows-modern"],
      "description": "Win32 `InitiateSystemShutdownExW` with `bRebootAfterShutdown=FALSE`. Honours `delay_seconds`, `force_close_apps` (EWX_FORCE), `reason` (mapped to SHTDN_REASON_* constant)."
    },
    {
      "name": "exit_windows_ex",
      "detect": { "type": "always" },
      "available_on": ["windows-classic"],
      "description": "Win32 `ExitWindowsEx(EWX_SHUTDOWN [| EWX_FORCE])`. Available on NT 4 / 9x onwards. On 9x without admin this is a no-op-with-error rather than a silent fail."
    }
  ],
  "x-families": {
    "windows-modern": {
      "description": "InitiateSystemShutdownExW with delay_seconds, force_close_apps, and reason mapped through. Requires SeShutdownPrivilege (auto-enabled if held). Connection drops once the OS issues the shutdown.",
      "implementations_in_order": ["initiate_system_shutdown_ex"]
    },
    "windows-classic": {
      "description": "ExitWindowsEx(EWX_SHUTDOWN [| EWX_FORCE]) on NT 4 / 2000 / XP / Vista / 7 / 8.x. On 9x without admin this is a no-op-with-error rather than a silent fail.",
      "implementations_in_order": ["exit_windows_ex"]
    }
  }
}
